/*
This class will be good for web user update - provides a single web user plus the list of user Roles (for pick list).
 */
package model.Resume;

import model.Resume.*;

public class ResumeWithRoles {
    
    public StringDataListResume resume = new StringDataListResume();
    public model.email.StringDataList emailInfo = new model.email.StringDataList();
    
}